<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.maven.mapper.OrderDetailMapper">

    <!-- 根据订单ID查询明细列表（关联商品名称） -->
    <select id="selectByOrderId" resultType="orderDetail">
        SELECT d.*,
               p.name AS product_name
        FROM easybuy_order_detail d
        LEFT JOIN easybuy_product p ON p.id = d.product_id
        WHERE d.order_id = #{orderId}
    </select>

    <!-- 根据ID查询 -->
    <select id="selectById" resultType="orderDetail">
        SELECT *
        FROM easybuy_order_detail
        WHERE id = #{id}
    </select>

    <!-- 新增 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO easybuy_order_detail (order_id, product_id, quantity, cost)
        VALUES
            (#{orderId}, #{productId}, #{quantity}, #{cost})
    </insert>

    <!-- 批量新增 -->
    <insert id="batchInsert">
        INSERT INTO easybuy_order_detail (order_id, product_id, quantity, cost)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.quantity}, #{item.cost})
        </foreach>
    </insert>

    <!-- 退款：累加明细已退数量 -->
    <update id="addRefundQuantity">
        UPDATE easybuy_order_detail
        SET refund_quantity = refund_quantity + #{quantity}
        WHERE id = #{id}
          AND quantity - refund_quantity >= #{quantity}
    </update>

    <!-- 根据订单ID删除 -->
    <delete id="deleteByOrderId">
        DELETE
        FROM easybuy_order_detail
        WHERE order_id = #{orderId}
    </delete>

</mapper>
