<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.maven.mapper.UserMapper">

    <!-- 分页条件查询 -->
    <select id="selectPage" resultType="user">
        SELECT * FROM easybuy_user
        <where>
            is_delete = 0
            <if test="loginName != null and loginName != ''">
                AND login_name LIKE CONCAT('%', #{loginName}, '%')
            </if>
            <if test="startTime != null and startTime != ''">
                AND create_time &gt;= #{startTime}
            </if>
            <if test="endTime != null and endTime != ''">
                AND create_time &lt;= #{endTime}
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <!-- 根据ID查询 -->
    <select id="selectById" resultType="user">
        SELECT *
        FROM easybuy_user
        WHERE id = #{id}
          AND is_delete = 0
    </select>

    <!-- 根据登录名查询 -->
    <select id="selectByLoginName" resultType="user">
        SELECT *
        FROM easybuy_user
        WHERE login_name = #{loginName}
          AND is_delete = 0
    </select>

    <!-- 根据邮箱查询 -->
    <select id="selectByEmail" resultType="user">
        SELECT *
        FROM easybuy_user
        WHERE email = #{email}
          AND is_delete = 0
        LIMIT 1
    </select>

    <!-- 新增 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO easybuy_user (login_name, user_name, password, sex, identity_code, email, mobile, type, is_delete)
        VALUES
            (#{loginName}, #{userName}, #{password}, #{sex}, #{identityCode}, #{email}, #{mobile}, #{type}, 0)
    </insert>

    <!-- 修改 -->
    <update id="update">
        UPDATE easybuy_user
        <set>
            <if test="userName != null">
                user_name = #{userName},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="sex != null">
                sex = #{sex},
            </if>
            <if test="identityCode != null">
                identity_code = #{identityCode},
            </if>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="mobile != null">
                mobile = #{mobile},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
        </set>
        WHERE id = #{id} AND is_delete = 0
    </update>

    <!-- 逻辑删除 -->
    <update id="deleteById">
        UPDATE easybuy_user
        SET is_delete = 1
        WHERE id = #{id}
    </update>

</mapper>
